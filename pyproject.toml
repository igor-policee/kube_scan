[tool.poetry]
name = "kube-scan"
version = "0.1.0"
description = ""
authors = ["Igor Policee <ipolishchuk@astralinux.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
kubernetes = "^31.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
